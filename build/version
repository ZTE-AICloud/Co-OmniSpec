# 语义化版本控制（Semantic Versioning）规范
# 格式：vx.y.z
## 主版本号(x)：当软件进行了不兼容的 API 修改或重大架构变更时递增，例如从 1.2.3 升级到 2.0.0。递增后，次版本号和修订号需要归零。
## 次版本号(y)：当软件以向后兼容的方式新增了功能时递增，例如从 1.2.3 升级到 1.3.0。递增后，修订号需要归零。
## 修订号(z)：当软件进行了向后兼容的问题修正或小变动时递增，例如从 1.2.3 升级到 1.2.4。
version=v2.0.0

changes=```text
- 🎉 OmniSpec2.0.0 基线版本
- ✅ **版本号**: v2.0.0
- ✅ **基线版本**: 基于OmniSpec/origin/master进行skill重构
- ✅ **支持安装脚本**: 支持安装脚本，支持安装到指定Agent目录
- ✅ **支持发布构建脚本**: 支持发布构建脚本，支持发布到指定路径
```
