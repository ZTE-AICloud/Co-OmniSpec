# Token管理和上下文控制指南（深度逻辑架构）

## 基本限制

- 输入 + 输出总量不得超过模型上下文限制。
- 单次分析建议输入控制在 15 万 tokens 内。

## 上下文策略

1. 阶段开始前清理上一阶段上下文。
2. 优先使用 LSP 与结构化信号，避免整仓全文读取。
3. 对大目录分批扫描，按模块逐步归纳。

## 输出策略

- 只输出必要摘要与目标文件路径。
- 详细结果写入 `omni-doc/on-demand/logic_architecture.md`，避免在对话中展开大段内容。
