# 约束规则配置向导 - 步骤2/3

您选择了以下接口类型：
{{selected_interface_types}}

可选的约束规则配置：

### 文件路径过滤（可选）
请输入要包含的文件路径模式（如src/api/*）：
```
{{include_paths}}
```

### 函数名模式（可选）
请输入要匹配的函数名模式（如get_*、post_*）：
```
{{function_patterns}}
```

### 排除模式（可选）
请输入要排除的文件或函数模式：
```
{{exclude_patterns}}
```

[S] 跳过此步骤并使用默认配置，[Enter] 继续配置

您的选择：