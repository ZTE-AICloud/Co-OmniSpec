# 实施计划: [FEATURE]

**分支**: `[###-feature-name]` | **日期**: [DATE] | **规范**: [link]
**输入**: 来自 `/changes/[###-feature-name]/spec.md` 的功能规范

**注意**: 此模板由 `/design` 命令填充.

## 摘要

[从功能规范中提取: 主要需求 + 研究得出的技术方法]

## 技术背景

<!--
  需要操作: 将此部分内容替换为项目的技术细节.
  此处的结构以咨询性质呈现, 用于指导迭代过程.
-->

**语言/版本**: [例如: Python 3.11、Swift 5.9、Rust 1.75 或 NEEDS CLARIFICATION]
**主要依赖**: [例如: FastAPI、UIKit、LLVM 或 NEEDS CLARIFICATION]
**存储**: [如适用, 例如: PostgreSQL、CoreData、文件 或 N/A]
**测试**: [例如: pytest、XCTest、cargo test 或 NEEDS CLARIFICATION]
**目标平台**: [例如: Linux 服务器、iOS 15+、WASM 或 NEEDS CLARIFICATION]
**项目类型**: [单一/网页/移动 - 决定源代码结构]
**性能目标**: [领域特定, 例如: 1000 请求/秒、10k 行/秒、60 fps 或 NEEDS CLARIFICATION]
**约束条件**: [领域特定, 例如: <200ms p95、<100MB 内存、离线可用 或 NEEDS CLARIFICATION]
**规模/范围**: [领域特定, 例如: 10k 用户、1M 行代码、50 个屏幕 或 NEEDS CLARIFICATION]

## 波及文件与复用分析

*章程要求：设计阶段须列出本功能波及的文件，并标明现有实现可否复用；任务阶段将据此再确认是否真的要改。*

| 路径（源码/配置等） | 与本功能关系 | 现有实现是否已满足需求 | 复用/变更策略 | 关键符号（类型、函数等，可选） |
|---------------------|--------------|------------------------|---------------|--------------------------------|
| [例如: `src/services/foo.py`] | [调用链/数据流/配置项…] | [是，仅引用 / 部分满足，需扩展 / 否，需改] | [直接复用 / 小改扩展 / 必须修改] | [例如: `FooService.process`] |
| … | … | … | … | … |

**说明**：「波及」指数据流、调用链、配置或契约确实经过或依赖该文件；**不**将未验证的猜测路径列入「待改」。

## 章程检查

*门控: 必须在阶段 0 研究前通过. 阶段 1 设计后重新检查. *

须对照 `.infra/memory/constitution.md`（含**棕地架构连续性与复用优先**）填写；至少覆盖下列门控（可增删项目级条款）：

- [ ] **规格一致**：`spec.md` 未与既有逻辑架构、已有设计相矛盾；若有偏离已在规格中显式说明。
- [ ] **方案锚定现状**：优先在既有模块、对象与函数上演进；**禁止空对空新增设计/实现**；拟新增文件或新逻辑链已与章程对齐，且必要时已准备复杂度跟踪条目。
- [ ] **可定位修改点**：已列出拟变更的**现有**源码位置（路径、类型/对象、函数/方法），而非仅描述新目录或新概念模块。
- [ ] **波及与复用**：已填写「波及文件与复用分析」表；对可复用、无需修改的项已标明，避免任务阶段误排修改。
- [ ] **利旧决策顺序**：每个修改点已按 `参数兼容扩展 > 同文件新增函数 > 新增文件` 进行可行性论证；若跳过高优方案，已给出不可复用原因与替代方案比较。
- [ ] **扩散阈值检查**：预计修改文件数默认不超过 3；超过时已记录必要性、风险与回滚点，并明确为何不能收敛到更小改动面。

[基于章程文件确定的其他门控条件]

## 修改点严格检查

*门控: 每个修改点都必须填写，不满足项不得进入任务分解。*

| 修改点 | 支持状态（已支持/部分支持/不支持） | 利旧结论（参数兼容/同文件新函数/新增文件） | 最小化结论（目标文件数、目标符号） | 证据 | 风险/备注 |
|--------|------------------------------------|--------------------------------------------|------------------------------------|------|----------|
| [例如: 扩展消息处理逻辑] | [部分支持] | [同文件新函数] | [2 个文件；`src/x.py:handle`、`src/x.py:route`] | [context.md/代码入口] | [需回归路由分发] |
| … | … | … | … | … | … |

## 项目结构

### 文档(此功能)

```
changes/[###-feature]/
├── design.md              # 此文件 (/design 命令输出)
├── research.md          # 阶段 0 输出 (/design 命令)
├── data-model.md        # 阶段 1 输出 (/design 命令)
├── quickstart.md        # 阶段 1 输出 (/design 命令)
├── contracts/           # 阶段 1 输出 (/design 命令)
└── tasks.md             # 阶段 2 输出 (/tasks 命令 - 非 /design 创建)
```

### 源代码(仓库根目录)
<!--
  需要操作: 将下面的占位符树结构替换为此功能的具体布局.
  删除未使用的选项, 并使用真实路径(例如: apps/admin、packages/something)扩展所选结构.
  交付的计划不得包含选项标签.
-->

```
# [如未使用请删除] 选项 1: 单一项目(默认)
src/
├── models/
├── services/
├── cli/
└── lib/

tests/
├── contract/
├── integration/
└── unit/

# [如未使用请删除] 选项 2: Web 应用程序(检测到"前端" + "后端"时)
backend/
├── src/
│   ├── models/
│   ├── services/
│   └── api/
└── tests/

frontend/
├── src/
│   ├── components/
│   ├── pages/
│   └── services/
└── tests/

# [如未使用请删除] 选项 3: 移动端 + API(检测到 "iOS/Android" 时)
api/
└── [同上后端结构]

ios/ 或 android/
└── [平台特定结构: 功能模块、UI 流程、平台测试]
```

**结构决策**: [记录所选结构并引用上面捕获的真实目录]

## 复杂度跟踪

*仅在章程检查有必须证明的违规时填写*

| 违规 | 为什么需要 | 拒绝更简单替代方案的原因 |
|-----------|------------|-------------------------------------|
| [例如: 第 4 个项目] | [当前需求] | [为什么 3 个项目不够] |
| [例如: 仓储模式] | [特定问题] | [为什么直接数据库访问不够] |
