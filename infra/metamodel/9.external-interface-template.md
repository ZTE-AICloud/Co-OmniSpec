---
id: EXTERNAL-API-XXX
name: [EXTERNAL-API-XXX-接口名称]
file: [典型调用所在源文件路径，相对 REPO_ROOT，如 src/client/http_caller.py]
type: {类型，如 HTTP接口、其他}
description: [接口简要描述：一句话概括该外部接口的核心功能和用途；详细描述：说明在项目中的使用场景]
---

## 接口说明

### 接口描述
{一段话说明该外部接口的用途和在项目中的使用场景}

### 接口入口标识
[接口入口标识，如 模块.函数名、类名::方法名、RESTful路径等]

### 接口参数
| 参数名 | 类型 | 是否必需 | 默认值 | 说明 |
|--------|------|----------|--------|------|
| ...    | ...  | ...      | ...    | ...  |

### 波及调用链
- `调用链1(文件路径)` → 调用位置与作用简述
- `调用链2(文件路径)` → ...

