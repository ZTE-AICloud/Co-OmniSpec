# 深度架构识别报告

## 1. 项目概览

### 1.1 基本信息

- **项目名称**: {项目名称} ({项目英文名/简称})
- **技术栈**: {主语言/主框架，如 Java/Spring Boot} + {附属语言及职责占比，如 Lua(规则脚本~3%) + Shell(部署) + SQL(迁移)}
- **语言构成**: {主语言名称，如 Java}（~{主语言占比，如 92%}） + {附属语言1名称}（~{占比1}%） + {附属语言2名称}（~{占比2}%）…

> 🔴 **对齐约束（统一声明）**：以下 1.3 / 1.4 / 1.5 各节均须与 `stage2-search-coverage.json` 的 `languages` 逐项对齐，禁止遗漏或矛盾。
- **架构类型**: {架构类型描述,如: Web应用 + 插件式框架}
- **部署方式**: {部署方式,如: Django + Daphne (ASGI) + Nginx}
- **数据库**: {数据库类型}
- **分析目标**: {项目的核心定位/目标}

### 1.2 目录结构总览

```
{项目根目录}/
├── {模块1}/                    # {模块1说明}
├── {模块2}/                    # {模块2说明}
├── {模块3}/                    # {模块3说明}
│   ├── {子模块1}/
│   ├── {子模块2}/
│   └── {子模块3}/
├── {配置目录}/                 # {配置说明}
├── {数据目录}/                 # {数据说明}
├── {文件1}                     # {文件说明}
├── {文件2}                     # {文件说明}
└── {文件3}                     # {文件说明}
```

### 1.3 语言构成与附属语言影响面

> 本节合并呈现仓库所有代码语言的构成情况与附属语言影响面，与 `stage2-search-coverage.json` 的 `languages` 逐项对应。占比基于文件数/代码行数估算（二者差异大时同时列出）。YAML/JSON 视作配置语言计入。
> 仅 auxiliary 行需填「入口」「被谁调用」两列；primary 行填"—"。「被谁调用」记录跨语言调用边（JNI/subprocess/脚本引擎/命令行/ORM 触发）。

| 语言名称 | role | 文件后缀 | 文件数 | 代码行数 | 占比 | 主要作用 | 分析方式 | 入口（auxiliary） | 被谁调用（auxiliary） |
|----------|------|----------|--------|----------|------|----------|----------|-------------------|----------------------|
| {Java} | primary | `.java` | {N} | {~X 万行} | ~{占比1}% | 业务逻辑、领域模型、控制器 | lsp | — | — |
| {YAML} | auxiliary | `.yaml`, `.yml` | {N} | {~X 千行} | ~{占比2}% | 部署配置、CI/CD、环境参数 | read | — | —（配置被主语言读取） |
| {Lua} | auxiliary | `.lua` | {N} | {~X 千行} | ~{占比3}% | 规则脚本、热更新逻辑 | grep | `{文件路径}:{行号}` — {入口说明} | {主语言模块/文件:行号}，{脚本引擎 load/require} |
| {Shell} | auxiliary | `.sh` | {N} | {~X 百行} | ~{占比4}% | 部署脚本、CI/CD 流水线 | read | `{文件路径}:{行号}` — {入口说明} | {调用方}，{命令行/subprocess} |
| … | … | … | … | … | … | … | … | … | … |

> **注**：若某语言占比极低（<1%），可在"主要作用"中标注"零星脚本/配置片段"，并在"分析方式"标注 `manual`。附属语言即使占比小也必须记录其入口与被调用关系（防依赖孤岛）。

### 1.4 文件类型组成

> 本节按文件后缀分类统计仓库中所有文件类型，代码类后缀的"关联语言"须与 1.3 一致。本节只统计后缀分布，语言的"主要作用/入口/被谁调用"等语义信息以 1.3 为准，不在此重复。

| 文件后缀 | 类别 | 文件数 | 占比 | 关联语言 |
|----------|------|--------|------|----------|
| `.java` | 代码 | {N} | ~{占比}% | Java |
| `.yaml` / `.yml` | 配置 | {N} | ~{占比}% | YAML |
| `.lua` | 代码 | {N} | ~{占比}% | Lua |
| `.sh` | 代码 | {N} | ~{占比}% | Shell |
| `.md` | 文档 | {N} | ~{占比}% | — |
| `.json` | 配置 | {N} | ~{占比}% | — |
| `.xml` | 配置 | {N} | ~{占比}% | — |
| `.sql` | 代码 | {N} | ~{占比}% | SQL |
| `.proto` | 代码 | {N} | ~{占比}% | Protocol Buffers |
| `.go` | 代码 | {N} | ~{占比}% | Go |
| `.py` | 代码 | {N} | ~{占比}% | Python |
| `.txt` | 数据 | {N} | ~{占比}% | — |
| … | … | … | … | … |

> **类别说明**：代码（需计入语言构成）/ 配置 / 文档 / 数据 / 其他。若某后缀占比极低（<0.5%）可归入"其他"。文件数总和应与仓库实际文件数一致，百分比之和应约为 100%（允许舍入误差 ±1%）。

### 1.5 配置文件影响分析

> 本节汇总波及分析阶段（步骤5.2）产出的配置文件检索与解析结果，与 `stage2-static-asset-scan.json` 逐项对应，按后缀分组。消费者模块/函数应追溯到 `stage2-impact-candidates.json` 中标记为 `impact_type=direct` 或 `indirect` 的波及功能。

#### YAML 配置文件

| 配置文件 | 路径 | 关键配置项 | 配置用途 | 消费者模块/函数 | 关联波及功能 | 说明 |
|----------|------|-----------|---------|----------------|------------|------|
| {app.yaml} | config/app.yaml | server.port, auth.enabled | 应用启动参数 | src/main.go:LoadConfig | f_auth | — |
| {deployment.yaml} | k8s/deployment.yaml | replicas, image, ports.containerPort | K8s 部署参数 | —（纯基础设施配置） | — | 含变量引用 `${IMAGE_TAG}` |
| {ci.yaml} | .github/workflows/ci.yaml | on.push.branches, jobs.steps | CI 流水线定义 | — | — | — |
| … | … | … | … | … | … | … |

#### JSON 配置文件

| 配置文件 | 路径 | 关键字段 | 配置用途 | 消费者模块/函数 | 关联波及功能 | 说明 |
|----------|------|---------|---------|----------------|------------|------|
| {package.json} | package.json | dependencies, scripts | 依赖版本管理 | build.sh:install_deps | — | — |
| {tsconfig.json} | tsconfig.json | compilerOptions, include | TypeScript 编译配置 | —（构建工具消费） | — | — |
| … | … | … | … | … | … | … |

#### XML 配置文件

| 配置文件 | 路径 | 关键配置段 | 配置用途 | 消费者模块/函数 | 关联波及功能 | 说明 |
|----------|------|-----------|---------|----------------|------------|------|
| {web.xml} | src/main/webapp/WEB-INF/web.xml | context-param, servlet-mapping | Servlet 上下文配置 | org.springframework.web.ContextLoaderListener | f_spring_init | — |
| {pom.xml} | pom.xml | dependencies, plugins, properties | Maven 构建配置 | —（构建工具消费） | — | — |
| … | … | … | … | … | … | … |

#### properties / ini 配置文件

| 配置文件 | 路径 | 关键配置项 | 配置用途 | 消费者模块/函数 | 关联波及功能 | 说明 |
|----------|------|-----------|---------|----------------|------------|------|
| {application.properties} | src/main/resources/application.properties | jdbc.url, jpa.hibernate.ddl-auto | 数据库与 JPA 配置 | com.example.config.DataSourceConfig | f_datasource | — |
| {logging.ini} | config/logging.ini | handlers, level | 日志级别配置 | LogbackConfigurator | f_logging | — |
| … | … | … | … | … | … | … |

> **填写说明**：
> - "消费者模块/函数"：从 `stage2-static-asset-scan.json` 的 `consumer_refs` 提取，追溯到具体代码文件:行号；无消费者时填 `—` 并在"说明"列注明原因（纯基础设施/未建立关联/证据不足）。
> - "关联波及功能"：从 `stage2-impact-candidates.json` 匹配，配置若被某波及功能消费则填其 `function_key`；无关联则填 `—`。
> - 含变量引用的配置项（如 `${VAR}`、`{{template}}`）在"说明"列标注"含变量引用"；含敏感信息（密码/Token）在"说明"列标注"含敏感配置"。
> - 若某类配置文件（如 properties）数量多，可合并为一行（汇总路径），详细清单在 `stage2-static-asset-scan.json` 中查阅。
> - 若某后缀组配置文件数量多（如 10+ 个 properties 文件），可合并为一行，格式为"{后缀} 配置文件 ×{N}"，详细清单指向 `stage2-static-asset-scan.json`。

## 2. 架构类型识别

### 2.1 主体架构模式

**{主体架构模式名称}**

{项目名称}采用了{混合/单一}架构设计:

- **{层1名称}**: {层1职责和技术}
- **{层2名称}**: {层2职责和技术}
- **{层3名称}**: {层3职责和技术}
- **{层4名称}**: {层4职责和技术}
- **{层5名称}**: {层5职责和技术}

### 2.2 子架构模式

#### 2.2.1架构

- **框架**: {框架名称和路径}
- **模式**: {设计模式}
- **通信**: {通信方式}

#### 2.2.2架构

- **Web框架**: {框架名称和版本}
- **API设计**: {API风格}
- **认证**: {认证方式}
- **异步处理**: {异步处理方案}

#### 2.2.3架构

- **模式**: {架构模式}
- **组织**: {组织方式}
- **执行**: {执行方式}
- **结果**: {结果格式}

---

## 3. 模块与层级索引（可引用/可检索）

### 3.1 层级架构图

```
┌─────────────────────────────────────────────────────────┐
│                    {层级1名称}                            │
│           {层级1技术栈} ({路径})                          │
└─────────────────────────────────────────────────────────┘
                           ↓ {通信方式}
┌─────────────────────────────────────────────────────────┐
│                   {层级2名称}                             │
│                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │  {模块1}     │  │  {模块2}     │  │  {模块3}     │ │
│  │  {模块1说明} │  │  {模块2说明} │  │  {模块3说明} │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
│         {路径}                                           │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│                  {层级3名称}                              │
│                                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │              {子系统名称}                          │  │
│  │                                                    │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌──────────┐ │  │
│  │  │   {模块1}   │  │   {模块2}   │  │  {模块3} │ │  │
│  │  │  ({说明1})  │  │  ({说明2})  │  │ ({说明3})│ │  │
│  │  └─────────────┘  └─────────────┘  └──────────┘ │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                           ↓
[继续其他层级...]
```

### 3.2 模块层级详细说明

#### 层级 1: ()

- **路径**: `{路径}`
- **职责**: {职责描述}
- **技术**: {技术栈}
- **关键模块**:
  - `{模块1}/`: {模块1说明}
  - `{模块2}/`: {模块2说明}
  - `{模块3}/`: {模块3说明}

#### 层级 2: ()

- **路径**: `{路径}`
- **职责**: {职责描述}
- **技术**: {技术栈}
- **关键文件**:
  - `{文件1}`: {文件1说明}
  - `{文件2}`: {文件2说明}
  - `{文件3}`: {文件3说明}

#### 层级 3: ()

- **路径**: `{路径}`
- **职责**: {职责描述}
- **关键模块**:

##### 3.3.1 ()

- `{文件名}` ({行数}+ lines): {文件说明}
  - {功能1}: {函数名1}, {函数名2}
  - {功能2}: {函数名3}, {函数名4}
  - {功能3}: {函数名5}, {函数名6}

##### 3.3.2 ()

- `{文件名}`: {文件说明}
  - `{模型1}`: {模型说明} ({字段列表})
  - `{模型2}`: {模型说明}
  - `{模型3}`: {模型说明}

##### 3.3.3 ()

- `{文件名}`: {文件说明}
  - `{函数1}()`: {函数说明}
  - `{函数2}()`: {函数说明}
  - `{函数3}()`: {函数说明}

##### 3.3.4 ()

- `{文件1}`: {功能1} ({实现方式})
- `{文件2}`: {功能2}
- `{文件3}`: {功能3}

[继续其他层级的详细说明...]

---

## 4. 模块边界定义

### 4.1模块边界

#### 4.1.1 公共接口

- **路径**: `{路径}`
- **{接口类型}**:
  - `{接口1}`: {接口1说明}
  - `{接口2}`: {接口2说明}
  - `{接口3}`: {接口3说明}

#### 4.1.2 内部实现

- **路径**: `{路径}`
- **{模块类型}**: {模块说明}
- **{工具类}**: {工具说明}

#### 4.1.3 边界规则

- {规则1}
- {规则2}
- {规则3}

### 4.2模块边界

#### 4.2.1 公共接口

- **{接口类型1}**: `{配置文件}` 定义的所有{接口说明}

  - `{endpoint1}`: {功能说明}
  - `{endpoint2}`: {功能说明}
  - `{endpoint3}`: {功能说明}
- **{接口类型2}**: `{配置文件}`

  - `{endpoint}`: {功能说明}

#### 4.2.2 内部实现

- `{文件1}`: {功能1}
- `{文件2}`: {功能2}
- `{文件3}`: {功能3}

#### 4.2.3 边界规则

- {规则1}
- {规则2}
- {规则3}

[继续其他层级的边界定义...]

---

## 5. 模块依赖关系分析

### 5.1 依赖关系图

```
{层级1名称} ({技术})
    │
    ├─[{通信方式1}]→ {层级2名称} ({技术})
    │
    └─[{通信方式2}]→ {其他模块}

{层级2名称} ({模块说明})
    │
    ├─[{调用方式1}]→ {层级3名称} ({模块})
    │
    └─[{调用方式2}]→ {其他模块}

{层级3名称} ({路径})
    │
    ├─[{访问方式1}]→ {模块1} ({说明})
    │
    ├─[{调用方式}]→ {模块2} ({说明})
    │
    ├─[{调用方式}]→ {模块3} ({说明})
    │   ├── {子模块1}
    │   ├── {子模块2}
    │   └── {子模块3}
    │
    └─[{读取方式}]→ {配置文件}

[继续其他依赖关系...]
```

---

## 7. 安全架构

### 7.1 认证与授权

#### 7.1.1 认证机制

- **{认证方式1}**: {说明1}
- **{认证方式2}**: {说明2}
- **{认证方式3}**: {说明3}

#### 7.1.2 令牌管理

- **{令牌1}**: {有效期}
- **{令牌2}**: {有效期}
- **存储**: {存储方式}

### 7.2 数据安全

#### 7.2.1 加密

- **{数据类型1}加密**: {加密方式}
- **{数据类型2}加密**: {加密方式}
- **传输安全**: {传输安全方案}

#### 7.2.2保护

- **{框架/工具} {机制}**: {状态}
- **{配置}设置**:
  - {配置项1} = {值1}
  - {配置项2} = {值2}
  - {配置项3} = {值3}

### 7.3 访问控制

#### 7.3.1保护

- **{模块1}**: {功能1}
- **{模块2}**: {功能2}

#### 7.3.2 权限控制

- **{权限策略1}**: {说明1}
- **{限制机制}**: {说明2} ({配置项列表})

### 7.4 安全漏洞防护

#### 7.4.1防护

- `{文件/模块}`: {防护措施}
- {具体防护方法}

#### 7.4.2防护

- {防护措施1}
- {防护措施2}

---

## 8. 日志与监控

### 8.1 日志体系

#### 8.1.1

- **路径**: `{路径}`
- **文件**:
  - `{文件1}`: {说明1}
  - `{文件2}`: {说明2}
  - `{文件3}`: {说明3}

#### 8.1.2 日志配置

- **Handlers**: {Handler类型}
- **大小限制**: {大小}
- **备份数量**: {数量}
- **日志级别**: {级别列表}

#### 8.1.3

- **路径**: `{路径}`
- **内容**: {内容说明}

---

## 9. 测试架构

### 9.1 测试分层

```
{测试目录}/
├── {测试类型1}/              # {说明1}
│   ├── {模块1}/         # {模块1测试}
│   ├── {模块2}/       # {模块2测试}
│   └── ...
├── {测试类型2}/       # {说明2}
│   ├── {模块1}/
│   └── {模块2}/
└── {测试类型3}/              # {说明3}
    ├── {测试文件1}
    └── {测试目录1}/
```

### 9.2 测试框架（重点分析）

#### 9.2.1 单元测试框架

- **框架名称**: {框架名称，如Jest/pytest/JUnit等}
- **版本**: {版本号}
- **配置文件**: `{配置文件路径}`
- **特点**: {框架特点，如断言库、异步支持、快照测试等}

#### 9.2.2 集成测试框架

- **框架名称**: {框架名称}
- **版本**: {版本号}
- **配置文件**: `{配置文件路径}`
- **特点**: {框架特点}

#### 9.2.3 E2E测试框架（如存在）

- **框架名称**: {框架名称，如Cypress/Playwright/Selenium/Puppeteer等}
- **版本**: {版本号}
- **配置文件**: `{配置文件路径}`
- **浏览器支持**: {支持的浏览器}
- **特点**: {框架特点，如无头模式、录制回放、并行执行等}

#### 9.2.4 性能测试框架（如存在）

- **框架名称**: {框架名称，如JMeter/k6/Artillery等}
- **版本**: {版本号}
- **配置文件**: `{配置文件路径}`
- **特点**: {框架特点}

#### 9.2.5 测试运行器

- **运行器**: {测试运行器，如Jest runner/pytest runner/Maven Surefire等}
- **并行执行**: {是否支持，配置方式}
- **测试发现机制**: {如何发现测试用例}

### 9.3 Mock机制（重点分析）

#### 9.3.1 Mock框架/库

- **框架名称**: {Mock框架，如Mockito/unittest.mock/sinon/jest.fn等}
- **版本**: {版本号}
- **使用位置**: `{主要使用模块/目录}`

#### 9.3.2 Mock策略

- **接口Mock**: {是否使用，说明}
  - 方式: {HTTP mock/API mock/服务mock等}
  - 工具/库: {使用的工具}
- **数据库Mock**: {是否使用，说明}
  - 方式: {内存数据库/事务回滚/测试数据库等}
  - 工具/库: {使用的工具}
- **外部服务Mock**: {是否使用，说明}
  - 方式: {服务桩/容器化服务/测试替身等}
  - 工具/库: {使用的工具}
- **文件系统Mock**: {是否使用，说明}
  - 方式: {临时文件/内存文件系统等}
- **其他Mock**: {其他Mock场景}

#### 9.3.3 Mock数据来源和构造

- **Fixture文件**: {是否使用，路径 `{fixture路径}`}
- **Factory模式**: {是否使用，说明}
- **Inline Mock**: {是否使用，说明}
- **Mock数据构造方式**: {如何构造mock数据}

#### 9.3.4 Stub/Spy使用

- **Stub使用**: {是否使用，说明}
- **Spy使用**: {是否使用，说明}
- **Mock vs Stub vs Spy**: {项目中如何区分使用}

### 9.4 测试数据管理

#### 9.4.1 测试数据来源

- **Fixture文件**: {是否使用，路径 `{fixture路径}`}
- **Factory模式**: {是否使用，说明}
- **Seed数据**: {是否使用，说明}
- **Test Data Builders**: {是否使用，说明}
- **其他方式**: {其他数据来源}

#### 9.4.2 测试数据隔离策略

- **策略**: {每个测试独立数据/共享数据/事务回滚等}
- **实现方式**: {如何实现隔离}

#### 9.4.3 测试数据清理机制

- **清理方式**: {teardown/afterEach/事务回滚/数据库重置等}
- **清理时机**: {何时清理}

### 9.5 测试配置与环境

#### 9.5.1 测试配置文件

- **配置文件位置**: `{配置文件路径}`
- **配置内容**: {主要配置项说明}

#### 9.5.2 环境变量管理

- **测试环境变量**: {如何管理测试环境变量}
- **环境隔离**: {不同测试环境如何隔离}

#### 9.5.3 CI/CD集成

- **CI/CD集成方式**: {如何在CI/CD中运行测试}
- **测试报告**: {测试报告生成位置和格式}

### 9.6 测试覆盖率

#### 9.6.1 覆盖率工具

- **工具名称**: {覆盖率工具，如coverage.py/istanbul/JaCoCo等}
- **版本**: {版本号}
- **配置**: `{配置文件路径}`

#### 9.6.2 覆盖率目标

- **覆盖率目标**: {目标覆盖率，如80%}
- **覆盖率报告位置**: `{报告路径}`

### 9.7 测试工具（汇总）

- **{测试类型1}**: {工具名称}
- **{测试类型2}**: {工具名称}
- **{测试类型3}**: {工具名称}

---

<!-- 术语表已移除：如需术语统一，请在项目级文档或知识库中维护 -->
