---
description: Skill 开发规范，编写或修改 agent skills/ 下的 SKILL.md 及其补充文件时使用
globs: skills/**/SKILL.md,skills/**/*.md
alwaysApply: false
---

# Skill 开发规范

## 运行时模型（渐进式披露）

理解此模型是写好 skill 的前提：

1. **发现**：启动时仅加载 `name` + `description`，据此判断是否相关
2. **激活**：任务匹配时，完整 SKILL.md 加载到上下文
3. **执行**：按指令执行，按需加载引用文件或运行脚本

> 核心推论：`description` 决定激活时机——写不好就永远不会被调用。

### 拆分原则

- **职责单一**：一个 skill 做一件事
- **可扩展**：新增能力时不需要改已有 skill
- **粒度适中**：禁止过细拆分（如把读文件、写文件拆成两个 skill）

**不拆分**——满足以下全部条件时保持为单个 skill：

- 行数远低于上限（300 行）
- 多个步骤属于同一线性工作流的阶段，非独立功能模块
- 内部规则/模板/清单与主流程紧密耦合，拆出去无其他 skill 复用

**需拆分**——出现以下任一信号时考虑拆分或抽为引用文件：

- 某子部分（如检测规则、分类法）膨胀至 100+ 行
- 出现多套可切换的模板/格式
- 某子部分可被其他 skill 独立复用

## 目录结构

```txt
{AGENT_DIR}/skills/omni-doing-something/
  SKILL.md          # 主文件（必需）
  scripts/          # 可执行脚本（可选）
  references/       # 参考文档（可选）
  assets/           # 模板、资源（可选）
```

按需添加目录，不要过度设计：

- `scripts/` — 有确定性操作（API 调用、文件处理、数据转换）时添加
- `references/` — 步骤文档 >50 行或需要提示词模板时添加
- `assets/` — 需要输出模板、配置预设时添加

## 编写规范

### 元数据（YAML frontmatter）

- **`name`**：小写 kebab-case，`omni-` 前缀，2-5 词，优先动名词(-ing)，与目录名**一致**；1-64 字符，禁止首尾连字符、连续连字符(`--`)
- **`description`**：`职责描述. 触发条件.`（1-1024 字符）— 服务于发现阶段匹配，先述职责再述时机；含特定关键词帮助代理识别相关任务
- 禁止：泛化词（Helper、Utils）、英文 description、罗列流程步骤

✅ `name: omni-clarifying` + `description: 规范澄清技能. 通过澄清问题识别规范中未充分说明的领域. 当用户提到澄清规范、消除模糊性时使用.`
❌ `name: clarify` — 过短无前缀 | `description: 澄清规范` — 过于简短，匹配不稳定

### 正文结构

**格式没有刚性限制**，写有助于代理有效完成任务的内容即可。

推荐章节（按需选用，顺序保持一致）：

| 章节 | 必选 | 说明 |
| ------ | ------ | ------ |
| H1 标题 + 概述段落 | ✅ | 技能名称或功能描述作标题，紧跟 1-2 句概述 |
| 输入 | 推荐 | 含用户参数 `$ARGUMENTS`、文件、配置等 |
| 输出 | 推荐 | 产出制品与文件路径 |
| 执行流程 / 指令 | ✅ | 分步说明（`### N. 步骤名`），每步含验证检查点 |
| 关键规则 | 推荐 | 不可违反的约束与边界情况 |

### 执行者选择

每个步骤须明确执行者——选对执行者是效率关键：

- **脚本**：确定性操作（API 调用、文件读写、格式转换）→ 零上下文成本，结果稳定
- **Agent 指令**：需要 AI 判断的任务（理解、评估、生成、创作）→ 消耗上下文但能处理模糊任务

> 原则：能用脚本就不用 Agent。脚本返回极简 JSON（如 `{"ok": true, "count": 30}`），禁止输出详细日志。

### 验证检查点

每个关键步骤应包含验证条件，分层递进：

1. **文件存在**且非空
2. **格式正确**（JSON/MD 可解析）
3. **字段完整**（必要字段存在）
4. **值有效**（满足业务规则）

### 规模控制

- SKILL.md 控制在 **300 行以内**；超出按职责拆分为独立 skill 或引用文件
- 交叉引用代替重复；假设读者（AI）聪明

### Fewshot 骨架

```markdown
---
name: omni-doing-something
description: 某项任务的执行技能. 从 X 产出 Y. 当用户提到做某事时使用.
---

# omni-doing-something

为 Agent 提供某项任务的完整执行指令，产出可交付的 Y 制品。

## 输入

- **用户参数**：`$ARGUMENTS`（功能描述文本）
- **配置文件**：`.specify/memory/constitution.md`

## 输出

- `FEATURE_DIR/output.md`：产出制品

## 指令

### 1. 环境准备

- 运行对应脚本获取配置，解析所需变量

### 2. 执行核心逻辑

- 按模板填充，遵循 [reference.md](reference.md) 格式规则

### 3. 完成报告

- 输出：文件路径、结果摘要、下一步建议

## 关键规则

- 使用绝对路径操作文件
```

## 引用规则

- **跨技能**：`调用技能 \`skill-name\``，禁止绝对路径
- **同目录文件**：`[reference.md](reference.md)` 相对路径
- **仓库文件**：仓库根相对路径如 `.specify/templates/spec-template.md`
- **引用深度**：保持一层深度（SKILL.md → references/xxx.md），避免深层嵌套引用链
- **路径可执行性**：指令中须明确当前 skill 目录上下文，确保代理能定位文件

## DoD（完成校验）

交付前逐项验证，任一不通过则不可交付：

- [ ] `name` = 目录名？`omni-` 前缀？
- [ ] `description` 能否让代理仅凭此句判断是否激活？
- [ ] SKILL.md ≤ 300 行？
- [ ] 所有文件引用路径实际可达？
- [ ] 拆分粒度合理？无过细拆分，也没有需要拆分但是没拆的？
