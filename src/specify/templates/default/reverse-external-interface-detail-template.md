---
description: 外部依赖接口单文档模板，用于生成 EXTERNAL-API_{序号}_{描述}.md
---

#type {类型，如 HTTP接口、其他}
#file {典型调用所在源文件路径，相对 REPO_ROOT}
#content
## 接口名称
{接口名称，如 类名::方法名 或 模块.函数名}

## 整体接口描述
{一段话说明该外部接口的用途和在项目中的使用场景}

## 接口函数
{在代码中实际调用的名称，如 client.doPost}

## 接口参数
| 参数名 | 类型 | 是否必需 | 默认值 | 说明 |
|--------|------|----------|--------|------|
| ...    | ...  | ...      | ...    | ...  |

## 波及调用链
- `调用链1(文件路径)` → 调用位置与作用简述
- `调用链2(文件路径)` → ...
